Drop your own character art here.

-> name it to match `WaifuImage` in `@Resources/Variables.inc` (default: `waifu.png`)
-> transparent PNGs look best as cutouts; a JPG works too but shows a rectangle
-> images in this folder are git-ignored on purpose (don't commit art you don't own)
